[package]
name = "prodex-context"
version.workspace = true
edition.workspace = true
description = "Mojo-backed critical-signal safety helpers for prodex"
license.workspace = true
repository.workspace = true

[lib]
name = "prodex_context"
path = "src/lib.rs"

[features]
default = ["mojo"]
mojo = []

[dependencies]
serde = { workspace = true }
prodex_mojo_core = { workspace = true, features = ["mojo-runtime", "mojo-rich"] }

[lints]
workspace = true
